import { createTheme, PaletteOptions } from "@mui/material";

export const theme = createTheme({
  palette: {
    gray: {
      500: "#121214",
      400: "#212529",
      300: "#343B41",
      200: "#868E96",
      100: "#F8F9FA",
    },
  } as PaletteOptions,
});
